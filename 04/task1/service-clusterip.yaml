apiVersion: v1
kind: Service
metadata:
  name: service-clusterip
spec:
  ports:
  - name: nginx
    port: 9001
    protocol: TCP
    targetPort: 80
  - name: dns-tcp
    port: 9002
    protocol: TCP
    targetPort: 8080
  selector:
    app: myapp
  type: ClusterIP







